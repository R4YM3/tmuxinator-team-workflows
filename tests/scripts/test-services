#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

if ! command -v bats >/dev/null 2>&1; then
  echo "[error] bats is required. Install bats-core to run tests." >&2
  exit 1
fi

cd "$ROOT_DIR"
bats -r tests/services/contract
